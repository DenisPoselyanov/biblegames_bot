# Повний гайд: Біблійна гра (для тих, хто знає HTML/CSS)

> **Для кого цей документ:** ти добре знаєш **HTML і CSS**, але **JavaScript знаєш слабо або майже не знаєш**.  
> Тут усе пояснено з нуля: що таке React/Vite, як читати файли проєкту і **що саме змінити**, щоб додати тему, питання або змінити текст.

**Головна думка:** ти не вчиш «нову мову з нуля». Ти вчиш **новий спосіб збирати той самий сайт**, який раніше був би `index.html` + `style.css` + `script.js`.

---

## Зміст

0. [Що тобі реально потрібно знати](#0-що-тобі-реально-потрібно-знати)
1. [HTML/CSS сайт vs цей проєкт](#1-htmlcss-сайт-vs-цей-проєкт)
2. [JavaScript — лише те, що зустрінеш тут](#2-javascript--лише-те-що-зустрінеш-тут)
3. [Як запустити проєкт](#3-як-запустити-проєкт)
4. [Структура папок (карта)](#4-структура-папок-карта)
5. [Як відкривається сторінка](#5-як-відкривається-сторінка)
6. [React простими словами](#6-react-простими-словами)
7. [Файли проєкту — що де лежить](#7-файли-проєкту--що-де-лежить)
8. [Як працює гра](#8-як-працює-гра)
9. [Практика: що відкрити і що написати](#9-практика-що-відкрити-і-що-написати)
10. [Порівняння зі звичайним HTML](#10-порівняння-зі-звичайним-html)
11. [Telegram Mini App](#11-telegram-mini-app)
12. [Збірка для публікації](#12-збірка-для-публікації)
13. [Словник](#13-словник)
14. [Шпаргалка «хочу змінити…»](#14-шпаргалка-хочу-змінити)
15. [План навчання (по кроках)](#15-план-навчання-по-кроках)
16. [Що нового у версії Study 2.0 (Phase 1)](#16-що-нового-у-версії-study-20-phase-1)
17. [Що нового у версії Phase 2 — Adaptive Study](#17-що-нового-у-версії-phase-2--adaptive-study)
18. [Що нового у версії Phase 3 — Content + Social](#18-що-нового-у-версії-phase-3--content--social)

---

## 0. Що тобі реально потрібно знати

| Твої знання | Як використовуються тут |
|-------------|------------------------|
| **HTML** | Розмітка в файлах `.tsx` виглядає майже як HTML |
| **CSS** | Файли `.css` — звичайні стилі, можна правити як завжди |
| **JS (мінімум)** | Змінні, масиви, функції, `if`, цикл `for` — розділ 2 |
| **Не обов’язково зараз** | Класи, async/await, складний DOM, jQuery |

### Що НЕ треба вміти на старті

- Писати складний JavaScript з нуля
- Розуміти все в `node_modules/`
- Знати TypeScript на 100% (достатньо читати підказки в редакторі)

### Що треба вміти робити

- Відкрити файл у редакторі
- Знайти текст і змінити його (як у HTML)
- Змінити число в одному місці (наприклад, кількість питань)
- Скопіювати блок і вставити змінений (наприклад, нове питання)
- Запустити `npm run dev` у терміналі

---

## 1. HTML/CSS сайт vs цей проєкт

### Звичайний сайт (те, що ти знаєш)

```
index.html     →  розмітка
style.css      →  стилі
script.js      →  логіка (кліки, рахунок, збереження)
```

Ти сам:
1. Знаходиш елемент: `document.getElementById('score')`
2. Міняєш текст: `element.textContent = '100'`
3. Ховаєш/показуєш блоки для «сторінок»
4. Зберігаєш у `localStorage`

### Цей проєкт

```
index.html          →  одна порожня сторінка з <div id="root">
src/pages/*.tsx     →  «сторінки» (як HTML, але в окремих файлах)
src/*.module.css    →  стилі (як CSS)
src/data/*.ts       →  дані: теми, питання (як JSON у коді)
```

| Звично | Тут |
|--------|-----|
| `index.html` | Багато файлів у `src/pages/` |
| `script.js` | Логіка всередині `.tsx` + файли `data/`, `lib/` |
| `style.css` | `index.css` + `Home.module.css`, `Quiz.module.css`… |
| Відкрив `about.html` | URL `/about` → React показує потрібний файл |

**Vite** — програма, яка:
- запускає сайт локально (`npm run dev`);
- після збереження файлу **сама оновлює** сторінку в браузері;
- збирає все в папку `dist/` для Telegram.

**React** — бібліотека: ти **не шукаєш елементи в DOM**. Ти змінюєш **дані** (рахунок, номер питання), а React **сам перемальовує** HTML.

---

## 2. JavaScript — лише те, що зустрінеш тут

Читай цей розділ повільно. Цього достатньо для 90% правок у проєкті.

### 2.1 Змінна — «коробка з значенням»

```javascript
let score = 0;           // число
let name = 'Іван';       // текст (в лапках)
const MAX = 7;           // константа — не змінюємо
```

**У проєкті:** `QUESTIONS_PER_LEVEL = 7` у файлі `types/index.ts` — це така «коробка».

---

### 2.2 Масив — список

```javascript
const fruits = ['яблуко', 'груша', 'слива'];
fruits[0];   // 'яблуко' — перший (рахунок з 0!)
fruits[1];   // 'груша'
```

**У проєкті:** список тем `THEMES`, список варіантів відповіді `['Йордан', 'Ніл', 'Євфрат', 'Тигр']`.

---

### 2.3 Об’єкт — запис з полями

```javascript
const theme = {
  id: 'geography',
  title: 'Географія',
  icon: '🗺️',
};
theme.title;   // 'Географія'
theme.id;      // 'geography'
```

**У проєкті:** кожна тема і кожне питання — об’єкт.

---

### 2.4 Функція — дія, яку можна викликати

```javascript
function подвоїти(x) {
  return x * 2;
}
подвоїти(5);   // 10
```

**У проєкті:** `getQuestionsForLevel(...)` — функція, яка повертає 7 випадкових питань.

---

### 2.5 `import` і `export` — підключити інший файл

**Аналог у HTML:** `<script src="storage.js"></script>`, тільки в сучасних проєктах так:

```javascript
// у файлі storage.ts — «віддаємо назовні»
export function saveProfile(data) { ... }

// у іншому файлі — «беремо»
import { saveProfile } from './lib/storage';
```

**Правило:** рядки `import ...` завжди **на початку файлу**. Не треба їх боятися — це просто «підключи інший файл».

---

### 2.6 Стрілка `=>` — короткий запис функції

Це **те саме**, що `function`, тільки коротше:

```javascript
// Довго:
function add(a, b) { return a + b; }

// Коротко:
const add = (a, b) => a + b;
```

У React часто пишуть:
```javascript
onClick={() => handleSelect(0)}
```
Це означає: «при кліку виконай `handleSelect(0)`».

---

### 2.7 `?.` — «якщо є, тоді візьми»

```javascript
user?.first_name
```
Якщо `user` немає — не буде помилки, буде `undefined`.  
**У проєкті:** ім’я з Telegram може бути порожнім.

---

### 2.8 Файли `.ts` і `.tsx` — що це

| Розширення | Що всередині |
|------------|--------------|
| `.ts` | JavaScript + підказки типів (допомагає редактору) |
| `.tsx` | JavaScript + **HTML всередині** (JSX) |
| `.css` | Звичайний CSS |

**TypeScript (`.ts`)** — це JavaScript з «перевіркою помилок». Якщо редактор підкреслює червоним — щось не так із типом. Для правок тексту і даних часто можна ігнорувати складні типи.

---

### 2.9 JSX — HTML у JavaScript

```tsx
const name = 'Марія';
return (
  <p>Мир тобі, {name}!</p>
);
```

- Теги `<p>`, `<div>` — як у HTML.
- `{name}` — вставити значення змінної (як шаблон).
- Замість `class` пишуть `className` (бо `class` зайняте в JS).

**Тобі достатньо:** правити текст у тегах і дивитися на `{змінні}`.

---

## 3. Як запустити проєкт

1. Відкрий термінал у папці `bible-game`.
2. Один раз:
   ```bash
   npm install
   ```
3. Кожного разу для роботи:
   ```bash
   npm run dev
   ```
4. У браузері відкрий адресу з терміналу (зазвичай `http://localhost:5173`).

| Команда | Навіщо |
|---------|--------|
| `npm run dev` | Розробка, автооновлення |
| `npm run build` | Зібрати для Telegram (папка `dist/`) |

**Якщо щось зламалось після правки:** подивись червоний текст у терміналі або в браузері (F12 → Console).

---

## 4. Структура папок (карта)

```
bible-game/
├── index.html              ← одна HTML-сторінка, порожній <div id="root">
├── GUIDE.md                ← цей файл
├── package.json            ← список команд (не чіпай без потреби)
│
└── src/                    ← ВСЕ, що ти редагуєш
    ├── main.tsx            ← старт: вставити гру в #root
    ├── App.tsx             ← які URL → який екран
    ├── index.css           ← кольори всього сайту
    │
    ├── pages/              ← ЕКРАНИ (як окремі html-сторінки)
    │   ├── Home.tsx            головна
    │   ├── Themes.tsx          список тем
    │   ├── ThemeDetail.tsx     вибір складності
    │   ├── Quiz.tsx            вікторина
    │   ├── Profile.tsx         профіль
    │   └── GlobalStats.tsx     рейтинг
    │
    ├── components/           ← шматки, що повторюються
    │   ├── Layout.tsx          нижнє меню
    │   └── ThemeCard.tsx       картка теми
    │
    ├── data/                 ← ДАНІ (тексти питань, теми)
    │   ├── themes.ts
    │   ├── questions.ts
    │   ├── questions-extra.ts
    │   └── questionDbLoader.ts  ← підвантаження AI JSON
    │
    ├── types/index.ts        ← числа: 7 питань, очки за рівень
    ├── context/PlayerContext.tsx  ← рахунок гравця
    ├── lib/storage.ts        ← збереження в браузері
    └── hooks/useTelegram.ts  ← ім’я з Telegram
```

### Просте правило

| Хочу змінити… | Папка |
|---------------|--------|
| Текст на екрані, кнопки | `pages/` + `.module.css` поруч |
| Колір, шрифт усього сайту | `index.css` |
| Список тем | `data/themes.ts` |
| Питання | `data/questions.ts`, `data/question-db/*.json`, `npm run generate-ai` |
| Скільки питань / очки | `types/index.ts` |
| Збереження прогресу | `lib/storage.ts` |

**Не відкривай:** `node_modules/`, `dist/` (це збирається автоматично).

---

## 5. Як відкривається сторінка

```
1. Браузер відкриває index.html
2. main.tsx каже: «намалюй <App /> у #root»
3. App.tsx дивиться на URL:
      /              → Home.tsx
      /themes        → Themes.tsx
      /themes/geography → ThemeDetail.tsx
      /quiz/geography/easy → Quiz.tsx
4. React показує потрібний файл з pages/
```

**Порівняння з HTML:** раніше ти б робив `quiz.html` або ховав `<div id="quiz">`. Тут адреса в браузері (`/quiz/...`) сама вибирає файл.

---

## 6. React простими словами

### 6.1 Компонент = шаблон сторінки

Файл `Home.tsx` — це **функція**, яка повертає HTML-подібний код.

```tsx
export function Home() {
  return (
    <section>
      <h1>Біблійна гра</h1>
    </section>
  );
}
```

**Ти можеш:** міняти текст у `<h1>`, додавати `<p>`, як у звичайному HTML.

---

### 6.2 Стан (state) — число/текст, який сам оновлює екран

**Раніше (JS):**
```javascript
let count = 0;
count++;
document.getElementById('count').textContent = count;
```

**Тут (React):**
```tsx
const [count, setCount] = useState(0);

// при кліку:
setCount(count + 1);   // React сам оновить текст на екрані
```

**Запам’ятай:** не шукай `getElementById` у `Quiz.tsx`. Шукай `useState` і `set...`.

---

### 6.3 Список тем — цикл `.map()` (замість копіювання HTML)

**Раніше:** 15 разів скопіював блок `<div class="card">`.

**Тут у `Themes.tsx`:**
```tsx
{THEMES.map((theme) => (
  <ThemeCard key={theme.id} theme={theme} />
))}
```

Це означає: «для кожної теми з масиву покажи картку».  
**Тобі достатньо:** додати об’єкт у `themes.ts` — картка з’явиться сама.

---

### 6.4 Context — «загальний зошит» для очок

Профіль гравця (очки, пройдені рівні) лежить у `PlayerContext.tsx`.

На будь-якій сторінці:
```tsx
const { profile } = usePlayer();
// profile.totalPoints — скільки очок
```

**Аналог:** один глобальний об’єкт `window.player`, тільки організовано.

---

## 7. Файли проєкту — що де лежить

### `types/index.ts` — налаштування гри (часто лише це треба)

```ts
export const QUESTIONS_PER_LEVEL = 7;   // скільки питань за один прохід

export const DIFFICULTY_POINTS = {
  beginner: 5,
  easy: 15,
  medium: 30,
  hard: 60,
  expert: 100,
};
```

Змінив число → зберег → перезавантаж сторінку в браузері.

---

### `data/themes.ts` — список тематик

Кожна тема — об’єкт у масиві. Додав об’єкт → тема з’явиться в грі.

---

### `data/questions.ts` + `questions-extra.ts` + `data/question-db/` — питання

- **questions.ts** — основні питання вручну
- **questions-extra.ts** — додаткові вручну
- **data/question-db/*.json** — тисячі питань від локальної AI (Ollama), див. `scripts/AI_SETUP.md`

**Як вибираються 7 питань:**
1. Збираються всі питання **цієї теми** і **цієї складності** (TS + JSON)
2. Перемішуються (випадковий порядок)
3. Беруться перші **7**

Кожен новий прохід рівня — **інший порядок** і **інша комбінація** з пулу.

---

### `pages/Quiz.tsx` — вікторина

- Питання і кнопки відповідей — **внизу** екрана (зручно для пальця в Telegram)
- Після відповіді — кнопка «Далі»
- В кінці — нарахування очок

---

### `lib/storage.ts` — збереження

Звичайний `localStorage` — як ти міг би писати в `script.js`. React тут майже не потрібен.

---

### Багато питань через AI (Ollama)

```bash
npm run generate-ai -- --theme geography --count 50
npm run questions:stats
```

Детально: **`scripts/AI_SETUP.md`**

---

## 8. Як працює гра

### Кроки для гравця

1. Обрати **тему** (Географія, Царі…)
2. Обрати **складність** (5 рівнів: від Початкового до Експерта)
3. Відповісти на **7 питань** (порядок випадковий)
4. Отримати **очки** в профіль

### Формула очок

```
очки = (очки за рівень) × (правильні відповіді ÷ 7)
```

Приклад: рівень «Складний» = максимум **60** очок.  
7 з 7 правильних → 60 очок.  
5 з 7 правильних → 60 × (5/7) ≈ **43** очки.

### 5 рівнів складності

| Рівень | Назва | Макс. очок |
|--------|-------|------------|
| beginner | Початковий | 5 |
| easy | Легкий | 15 |
| medium | Середній | 30 |
| hard | Складний | 60 |
| expert | Експерт | 100 |

---

## 9. Практика: що відкрити і що написати

### 9.1 Змінити текст на головній

1. Відкрий `src/pages/Home.tsx`
2. Знайди потрібний рядок у лапках, наприклад: `<h1>Біблійна гра</h1>`
3. Зміни текст, збережи (Ctrl+S)
4. Подивись у браузер — має оновитись сам

Стилі: `src/pages/Home.module.css`

---

### 9.2 Додати нову тему (без складного JS)

**Крок 1.** `src/data/themes.ts` — скопіюй блок іншої теми і зміни:

```ts
{
  id: 'angels',              // латиницею, для URL
  title: 'Ангели',
  description: 'Гавриїл, Михаїл та інші',
  icon: '👼',
  color: '#6b8fd4',
},
```

**Крок 2.** Додай хоча б кілька питань з `themeId: 'angels'` у `questions.ts` (див. 9.3).

Тема з’явиться в списку **сама**.

---

### 9.3 Додати одне питання (найпростіший спосіб)

Відкрий `src/data/questions.ts`, знайди функцію `q(` і **скопіюй** один рядок, наприклад:

```ts
q('geography', 'easy', 11, 'Твоє питання?', ['Вірно', 'Ні', 'Ні', 'Ні'], 0, 'Ін. 3:16'),
```

| Параметр | Що означає |
|----------|------------|
| `'geography'` | id теми |
| `'easy'` | складність: `beginner`, `easy`, `medium`, `hard`, `expert` |
| `11` | номер (унікальний для цієї теми+складності) |
| `'Твоє питання?'` | текст |
| `['Вірно', 'Ні', ...]` | 4 варіанти |
| `0` | який варіант правильний: 0=перший, 1=другий… |
| `'Ін. 3:16'` | посилання (можна прибрати) |

Збережи файл.

---

### 9.4 Змінити кількість питань (наприклад, 10 замість 7)

**Один файл:** `src/types/index.ts`

```ts
export const QUESTIONS_PER_LEVEL = 10;
```

У `ThemeDetail` текст «7 запитань» оновиться автоматично.

---

### 9.5 Змінити очки за рівень

**Файл:** `src/types/index.ts` → блок `DIFFICULTY_POINTS`. Зміни число, збережи.

---

### 9.6 Змінити кольори (як завжди в CSS)

**Файл:** `src/index.css`

```css
:root {
  --bg: #1a1208;        /* фон */
  --gold: #c9a227;      /* золотий акцент */
  --text: #f5f0e6;      /* текст */
}
```

Це звичайний CSS — тут ти вже на «своїй» території.

---

### 9.7 Вікторина: питання внизу екрана

| Що | Файл |
|----|------|
| Розмітка (що зверху, що знизу) | `pages/Quiz.tsx` |
| Вигляд нижньої панелі | `pages/Quiz.module.css` → клас `.bottomPanel` |

---

### 9.8 Додати багато питань одразу

1. Встанови [Ollama](https://ollama.com) і запусти `ollama serve`
2. У терміналі:
   ```bash
   npm run generate-ai -- --theme geography --count 100
   ```
3. Питання з’являться в `data/question-db/geography.json` — гра підхопить їх сама

---

## 10. Порівняння зі звичайним HTML

| Задача | HTML + CSS + JS (ти знаєш) | Цей проєкт |
|--------|----------------------------|------------|
| Змінити заголовок | Відкрив html, змінив `<h1>` | Відкрив `Home.tsx`, змінив `<h1>` |
| Змінити колір | `style.css` | `index.css` або `*.module.css` |
| Нова сторінка | Новий `.html` + посилання | Новий файл у `pages/` + рядок у `App.tsx` |
| Список карток | 15 разів HTML | Один масив у `themes.ts` |
| Рахунок очок | `getElementById` + `textContent` | `useState` / `usePlayer()` |
| Зберегти прогрес | `localStorage` вручну | `lib/storage.ts` (те саме) |
| Випадкові питання | Сам писав shuffle | `getQuestionsForLevel()` |

### Що спочатку здається складним

- `import` / `export`
- `useState`, `useEffect`
- Файли `.tsx`
- Термінал і `npm`

### Що потім стає **простіше**, ніж один великий `script.js`

- Додати тему — один об’єкт у масиві
- Змінити 7 → 10 питань — одне число
- Стилі не ламають один одного (`*.module.css`)
- Не губишся, «де оновлюється рахунок»

---

## 11. Telegram Mini App

Гра відкривається **в Telegram** (кнопка бота / Web App).

| Файл | Навіщо |
|------|--------|
| `hooks/useTelegram.ts` | Ім’я гравця |
| `lib/telegram.ts` | Підключення до Telegram |

У звичайному браузері (без Telegram) ім’я буде **«Гість»** — це нормально для перевірки.

**Публікація:** `npm run build` → завантаж папку `dist/` на HTTPS → у BotFather вкажи URL.

---

## 12. Збірка для публікації

```
Ти правиш файли в src/
        ↓
npm run build
        ↓
Готова папка dist/  →  завантажуєш на хостинг
```

Браузер **не розуміє** `.tsx` напряму. Vite перетворює все в звичайний `.js`, як твій `script.js`, тільки зібраний і стиснутий.

---

## 13. Словник

| Слово | Простими словами |
|-------|------------------|
| **npm** | Програма, що ставить бібліотеки і запускає команди |
| **Vite** | Сервер для розробки + збірка сайту |
| **React** | Малює сторінку, коли змінюються дані |
| **Компонент** | Файл-«шаблон» одного екрана або блоку |
| **JSX** | HTML всередині JavaScript |
| **State (стан)** | Змінна, після зміни якої оновлюється екран |
| **Props** | Дані, передані в компонент (як атрибути) |
| **Route** | Адреса в браузері → який екран показати |
| **localStorage** | Пам’ять браузера (зберігає очки) |
| **import** | Підключити код з іншого файлу |
| **export** | «Віддати» функцію/змінну іншим файлам |
| **map** | Цикл: зробити HTML для кожного елемента масиву |

---

## 14. Шпаргалка «хочу змінити…»

| Хочу… | Відкрий файл |
|-------|----------------|
| Текст на головній | `pages/Home.tsx` |
| Колір / шрифт усього сайту | `index.css` |
| Нову тему | `data/themes.ts` |
| Одне питання | `data/questions.ts` (скопіюй рядок `q(`) |
| Багато питань | `npm run generate-ai` (див. `scripts/AI_SETUP.md`) |
| 7 питань → інше число | `types/index.ts` → `QUESTIONS_PER_LEVEL` |
| Очки за рівень | `types/index.ts` → `DIFFICULTY_POINTS` |
| Вигляд вікторини | `pages/Quiz.module.css` |
| Нижнє меню | `components/Layout.tsx` |
| Новий екран | `pages/Новий.tsx` + `App.tsx` |
| URL сторінок | `App.tsx` |

---

## 15. План навчання (по кроках)

Рухайся **по порядку**. Не намагайся прочитати весь проєкт за день.

### Тиждень 1 — тільки HTML/CSS зона комфорту

1. `npm run dev`, поклацай усі екрани.
2. Відкрий `pages/Home.tsx` — зміни один заголовок, подивись результат.
3. Відкрий `index.css` — зміни `--gold` на інший колір.
4. Відкрий `pages/Quiz.module.css` — знайди `.bottomPanel`, зміни `padding`.

### Тиждень 2 — дані без React

5. Прочитай розділ 2 цього гайду (JS мінімум).
6. Відкрий `data/themes.ts` — знайди одну тему, прочитай поля.
7. Додай **тестову** тему з іконкою 🧪.
8. У `questions.ts` скопіюй один `q(...)` з новим текстом.

### Тиждень 3 — як все з’єднано

9. Відкрий `App.tsx` — подивись список шляхів `/themes`, `/profile`.
10. Відкрий `pages/Themes.tsx` — знайди `.map` і `THEMES`.
11. Відкрий `types/index.ts` — зміни `QUESTIONS_PER_LEVEL` на 5, перевір у грі.

### Коли будеш готовий (не раніше)

12. `pages/Quiz.tsx` — тільки знайди `useState` і кнопки відповідей.
13. `context/PlayerContext.tsx` — як рахуються очки.
14. `lib/storage.ts` — звичайний `localStorage`.

---

## 16. Що нового у версії Study 2.0 (Phase 1)

Ми щойно оновили гру з простої вікторини до справжньої навчальної платформи. Ось що змінилося простими словами:

1. **Дні поспіль (Streak):** Гра тепер запам'ятовує, чи заходиш ти кожного дня.
2. **Щоденні завдання (Daily Tasks):** На головному екрані тепер є місії (наприклад, пройти 3 рівні або відповісти на 10 питань).
3. **Рівень знань (Mastery):** Гра аналізує, які теми ти класно знаєш, а де часто помиляєшся.
4. **Нові пояснення:** До питань додано поля `explanationShort` та `explanationDeep`. Тепер після правильної чи неправильної відповіді гра може детально пояснити «Чому саме так».
5. **Справжня База Даних:** Раніше весь твій рахунок лежав тільки в пам'яті твого телефона (`localStorage`). Якщо ти міняв браузер — все зникало. Тепер гра вміє синхронізуватися зі справжнім сервером (PostgreSQL / Supabase). Зайшов з іншого пристрою — прогрес залишився з тобою!

---

## 17. Що нового у версії Phase 2 — Adaptive Study

Phase 2 додав інтелектуальні режими навчання, які адаптуються під твій прогрес:

### 17.1 Нові режими навчання в StudyHub

**StudyHub** — це новий центральний екран для вибору режимів навчання:

1. **Practice (Практика)** — звичайний режим навчання з питаннями на вибрану тему
2. **Review Mistakes (Робота над помилками)** — показує **тільки** ті питання, де ти раніше помилявся
3. **Sprint (Спринт)** — гра на час: 5 хвилин, дай як найбільше правильних відповідей

**Як це працює:**
- Гра аналізує твої помилки і помічає питання, де ти часто даєш неправильні відповіді
- У режимі "Review Mistakes" ти бачиш тільки ці питання, щоб закріпити матеріал
- У "Sprint" є таймер зверху екрану — коли час закінчується, гра автоматично завершується

### 17.2 GitHub-подібна heatmap в профілі

В екрані **Profile** тепер є візуалізація твого прогресу — кольорова сітка як у GitHub (де зелені квадратики показують активність). Тут це показує, які теми ти добре знаєш (зелений), а де треба повторити (жовтий/червоний).

### 17.3 Рекомендації тем

StudyHub тепер сам пропонує, які теми варто вивчити далі на основі твоїх результатів:
- Якщо ти часто помиляєшся в темі "Географія" — порадить повторити її
- Якщо добре знаєш "Патріархів" — пропустить і перейде до наступної

### 17.4 Нові досягнення

Додані досягнення за:
- Серію правильних відповідей (streak)
- Високий рівень знань (mastery)
- Завершені режими навчання

---

## 18. Що нового у версії Phase 3 — Content + Social

Phase 3 — це найбільше оновлення, яке перетворює гру з простої вікторини на повноцінну соціальну навчальну платформу.

### 18.1 Контроль якості питань (AI Quality Pipeline)

**Проблема раніше:** Якщо питання було поганим (нечітке, без посилання, дублікат), воно все одно потрапляло в гру.

**Що тепер:** Автоматична система перевіряє кожне питання перед тим, як показати його гравцеві.

**Як це працює:**
```bash
npm run analyze-quality  # Аналіз якості всіх питань
```

**Що перевіряється:**
- **Дублікати** — якщо питання дуже схоже на інше (наприклад, "У якій річці хрестив Іоан?" і "Хто хрестив у Йордані?") — помітить як дублікат
- **Неоднозначність** — якщо питання можна зрозуміти двозначно (наприклад, "Яке місто?" без контексту) — дасть низький бал
- **Посилання** — якщо немає біблійного посилання (наприклад, "Мат. 3:6") — помітить як проблему
- **Складність** — якщо питання помічено як "легке", але на нього мало хто відповідає правильно — система може запропонувати змінити складність

**Карантин:** Погані питання потрапляють в карантин і не показуються гравцям, поки адміністратор не виправить їх.

**Результати аналізу:**
- Всього питань: 225
- Схвалено: 223 (99.1%)
- В карантині: 2 (0.9%) — дублікати
- Потрібно доопрацювати: 41 (без посилань)

**Як користуватися:**
1. Запусти `npm run analyze-quality` — побачиш звіт у консолі
2. Звіт збережеться у файл `question-quality-report.json`
3. Якщо є питання в карантині — виправ їх у `src/data/questions.ts`

### 18.2 Розділення пулів питань (Study vs Game)

**Проблема раніше:** Одні й ті ж питання використовувалися і для навчання, і для розважальних ігор. Але для навчання потрібні якісні питання з поясненнями, а для ігор — швидкі та веселі.

**Що тепер:** Два окремих "баки" з питаннями:

**Study Pool (навчальний пул) — 184 питання:**
- Тільки з біблійними посиланнями
- Вищі вимоги до якості
- Використовується в режимі "Дослідження", "Practice", "Review Mistakes"
- Призначені для глибокого вивчення

**Game Pool (ігровий пул) — 225 питань:**
- Усі питання (навіть без посилань)
- М'якші вимоги до якості
- Використовується в "Міліонер", "Виживання", "Kahoot"
- Призначені для розваги

**Перекриття:** 184 питання підходять для обох пулів

**Як це працює автоматично:**
- Коли ти додаєш нове питання, сама система вирішує, в який пул його покласти
- Якщо питання має посилання і гарну якість — потрапить в обидва пули
- Якщо немає посилання — тільки в ігровий пул

**Як проаналізувати:**
```bash
npm run analyze-pools  # Аналіз пулів питань
```

**Як змінити правила:**
Відкрий файл `src/lib/questionPools.ts`, знайти `POOL_CONFIGS` і зміни:
```typescript
study: {
  minQualityScore: 60,        // мінімальна якість для навчання
  maxAmbiguityScore: 50,     // максимальна неоднозначність
  requireReference: true,     // чи обов'язкове посилання
  // ...
}
```

### 18.3 Kahoot Custom Playlists (Плейлисти)

**Нове:** Тепер гравці можуть створювати власні збірки питань для Kahoot — як плейлисти музики, тільки з біблійними питаннями.

**Що можна робити:**
- Створити плейлист "Різдвяні питання" або "Географія Святой Землі"
- Додати туди конкретні питання з різних тем
- Зробити плейлист публічним — щоб інші могли теж грати
- Лайкати плейлисти інших гравців
- Дивитися, скільки разів грав твій плейлист

**Як це працює в коді:**
```typescript
import { playlistManager } from '../src/lib/playlists';

// Створити плейлист
const playlist = playlistManager.createPlaylist(
  'Біблійна географія',
  'Найкращі питання про місця Біблії',
  'user123',
  'Тестовий користувач',
  ['geography-easy-1', 'geography-medium-5', 'geography-hard-3'],
  true // публічний
);
```

**Де використовується:**
- У Kahoot режимі гравці можуть вибирати не тільки випадкові питання, а й конкретні плейлисти
- Пошук плейлистів за назвою або темою
- Топ популярних плейлистів

### 18.4 Async Friend Challenges (Виклики друзів)

**Нове:** Тепер можна кидати виклики друзям — асинхронно. Не треба бути онлайн одночасно!

**Як це працює:**
1. Ти проходиш тему і отримуєш 85 балів
2. Кидаєш виклик другу: "Спробуй набрати більше!"
3. Друг отримує сповіщення і може прийняти виклик коли зручно
4. Друг проходить ті самі питання і отримує 78 балів
5. Система порівнює результати — ти виграв!

**Особливості:**
- Виклик діє 24 години
- Можна вказати тему і складність
- Можна обрати конкретні питання
- Історія всіх викликів і статистика перемог/поразок

**Як це працює в коді:**
```typescript
import { friendChallengeManager } from '../src/lib/friendChallenges';

// Створити виклик
const challenge = friendChallengeManager.createChallenge(
  'user1',              // хто кидає виклик
  'Тестовий користувач',
  85,                   // рахунок кидаючого
  'user2',              // кому кидають
  'Друг користувач',
  {
    themeId: 'geography',
    difficulty: 'medium',
    questions: ['geo-1', 'geo-2', 'geo-3']
  }
);

// Прийняти виклик
friendChallengeManager.acceptChallenge(challenge.id);

// Завершити виклик
friendChallengeManager.completeChallenge(challenge.id, 78);
```

### 18.5 Спільноти та лідерборди (Communities & Leaderboards)

**Нове:** Гравці можуть об'єднуватися в спільноти і змагатися в лідербордах.

**Що можна робити:**
- Створити спільноту "Біблійні дослідники" або "Молода пара"
- Запрошувати друзів або відкрити для всіх
- Лідерборд спільноти: хто найкращий за тиждень/місяць/за весь час
- Приватні спільноти за запрошенням
- Налаштування приватності профілю

**Соціальний профіль:**
- Список друзів
- Запити на дружбу
- Блокування користувачів
- Налаштування: показувати статистику, дозволити виклики, бути в лідербордах

**Як це працює в коді:**
```typescript
import { communityManager } from '../src/lib/communities';

// Створити спільноту
const community = communityManager.createCommunity(
  'Біблійні дослідники',
  'Група для вивчення Біблії разом',
  'user1',
  true // публічна
);

// Приєднатися
communityManager.joinCommunity(community.id, 'user2');

// Додати друга
communityManager.addFriend('user1', 'user2');

// Оновити лідерборд
communityManager.updateCommunityLeaderboard(community.id, [
  { userId: 'user1', displayName: 'Тестовий', score: 150, gamesPlayed: 5, accuracy: 85 },
  { userId: 'user2', displayName: 'Друг', score: 120, gamesPlayed: 4, accuracy: 78 }
], 'weekly');
```

### 18.6 Як тестувати нові функції

```bash
# Тест соціальних функцій
npm run test-social

# Тест аналізу якості
npm run analyze-quality

# Тест аналізу пулів
npm run analyze-pools
```

### 18.7 Структура нових файлів

```
src/lib/
├── questionQuality.ts      # Валідація якості питань
├── questionQuarantine.ts   # Карантин поганих питань
├── questionPools.ts        # Управління пулами (study/game)
├── playlists.ts            # Kahoot плейлисти
├── friendChallenges.ts    # Виклики друзів
└── communities.ts          # Спільноти та лідерборди

scripts/
├── analyzeQuestionQuality.ts   # Скрипт аналізу якості
├── analyzeQuestionPools.ts     # Скрипт аналізу пулів
├── testQuestionClassification.ts # Тест класифікації
└── testSocialFeatures.ts       # Тест соціальних функцій
```

### 18.8 Як додати нове питання з правильними полями

Раніше ти писав так:
```typescript
q('geography', 'easy', 1, 'У якій річці Іоанн хрестив?', ['Йордан', 'Ніл'], 0)
```

Тепер рекомендується додавати пояснення для кращої якості:
```typescript
q('geography', 'easy', 1, 'У якій річці Іоанн хрестив?', ['Йордан', 'Ніл', 'Євфрат', 'Тигр'], 0, 'Мат. 3:6')
```

Для питань, які мають потрапити в Study Pool, обов'язково:
- Посилання (четвертий параметр з кінця)
- Коротке пояснення (поки не реалізовано в UI, але готуй дані)

### 18.9 Інтеграція з UI

На даний момент усі функції Phase 3 реалізовані на рівні логіки (backend/lib). Для повноцінної роботи потрібно додати UI компоненти:

- **Екран плейлистів** — створення/редагування/перегляд плейлистів
- **Екран викликів** — список викликів, прийняття/відхилення
- **Екран спільнот** — створення/пошук/участь у спільнотах
- **Екран профілю** — соціальний профіль, друзі, налаштування
- **Екран лідербордів** — глобальний та спільнотні рейтинги

Це буде наступним етапом розробки.

---

## Якщо щось не працює

| Симптом | Що зробити |
|---------|------------|
| Білий екран | F12 → Console, читай червоний текст |
| Зміни не видно | Перевір, що зберіг файл; онови браузер |
| Помилка після `npm` | Переконайся, що термінал у папці `bible-game` |
| «Недостатньо питань» | Додай питання для цієї теми й складності в `questions.ts` |
| Не розумію файл | Спочатку шукай **текст у лапках** — його можна міняти як у HTML |

---

## Підсумок

- **HTML/CSS** ти вже вмієш — 70% правок це текст і стилі в звичних файлах.
- **JavaScript** тут — короткі шаблони: масив, об’єкт, `import`, `useState`. Розділ 2 — твоя шпаргалка.
- **React** — не магія: «змінив дані → екран оновився».
- **Найчастіші правки:** `themes.ts`, `questions.ts`, `types/index.ts`, `pages/*.tsx`, `*.css`.

Якщо застрягнеш — відкрий [розділ 14](#14-шпаргалка-хочу-змінити), знайди задачу в таблиці і відкрий лише один файл. Не треба розуміти весь проєкт одразу. 📖
