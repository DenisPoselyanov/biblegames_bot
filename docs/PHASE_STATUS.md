# Bible Games — статус канонічних фаз

> Канонічний порядок визначає [BIBLE_GAMES_MASTER_SPECIFICATION.md](./BIBLE_GAMES_MASTER_SPECIFICATION.md). Детальні execution-плани знаходяться в [`phases/`](./phases/README.md). Стек, MVP, domain boundaries і open-source reference rules уточнює [OPEN_SOURCE_REFERENCE_ARCHITECTURE.md](./OPEN_SOURCE_REFERENCE_ARCHITECTURE.md).

| Phase | Назва | Статус | Детальний план |
|---:|---|---|---|
| 0 | Canonical Documentation and Verified Baseline | completed — canonical docs migrated; reference architecture and GitHub search guide indexed | [Phase 0](./phases/PHASE_0_CANONICAL_DOCUMENTATION_AND_BASELINE.md) |
| 1 | Production Safety & Engineering Foundation | **completed (2026-09-07)** — WS1 (env validation, hardened Telegram initData verifier, typed principal, fail-closed HTTP + Socket auth, `createApp()` split, request-id + error envelope, Vitest + CI gates); WS2 (config-sourced RBAC, policy middleware, isolated + audited `/api/admin/questions`, append-only audit log, self-scoped `/api/v1/me/*`); WS3 (immutable wallet ledger + idempotency, server-authoritative `POST /api/v1/progression/completions` with stable `eventId`, preference/progression write split, one-time bounded migration, atomic JSON adapter); WS4 part 1 (practice-track + mastery + global-stats authority, `/progression/answers`, `/shop/purchases`, `/me/learning-state`; React client cutover); **WS4 part 2 (all rollout break-glass flags + legacy `x-user-id` / `PUT /profile` / `telegramAuth.ts` / dead `storage.ts` removed; in-memory rate limiting §13; structured logs + metrics + health/live+ready §16; demo routes isolated to non-production; DoD sign-off §23)**. `npm run check` green, 146 tests. | [Phase 1](./phases/PHASE_1_PRODUCTION_SAFETY_AND_ENGINEERING_FOUNDATION.md) |
| 2 | Core Architecture & Authoritative Data Platform | **DoD §26.1: 17/17 fully met** (2026-09-10). 5 stacked workstreams + WS6 decomposition; stack = Zod + Drizzle + pg-boss (ADR-012/013/014/016 accepted, ADR-015 object storage). **WS1–WS4 merged** (PRs #8 `b4d0a34`, #9 `009c5a2`, #10 `1ca59bd`, #11 `f25eca5`): `contracts/` runtime schemas + `validateBody` + §7.5 error envelope + port-free `createHttpServer`; Drizzle persistence + migration framework + identity/RBAC + persisted `RoleResolver` + shared rate-limit store; canonical `question_revisions` + `CANONICAL_CONTENT_REPOSITORY` cutover + realtime gateway v2 (`REALTIME_GATEWAY_V2`); frontend typed API client + query-key factory + provider decomposition + `PlayerContext` retired. **WS5** (`phase-2/ws5-jobs-storage-deploy`): background `JobQueue` + worker + retention sweeps (ADR-014; durable pg-boss adapter deferred, in-memory default); `ObjectStore` filesystem/S3 adapters + `content.snapshot` job (ADR-015); `docs/DEPLOYMENT.md` + `docs/OBSERVABILITY.md` + `docs/ROLLOUT_PHASE_2.md`; HTTP/DB metrics + frontend error reporting; typed `user_preferences` cutover + `LEGACY_STORE_READONLY` + backfill script. **WS6** (`phase-2/progression-decomposition`, ADR-016, 2026-09-10): progression + entitlement decomposed into typed `progression_state` / `achievement_grants` / `player_theme_stats` / `entitlements` tables (migrations 0004–0006), transactional reward hot path (`db.transaction` + `FOR UPDATE`), `legacyBlobMirror` dual-write behind `LEGACY_PROGRESSION_READONLY`, `migrate:backfill-progression` + provenance/verification, and `productionValidation.ts` retires `STORAGE_PROVIDER=json` — closes DoD #6/#7/#9. Server-side verified (330 tests); full `npm run check` pending a clean `npm ci` (local node_modules corrupted, unrelated). **Rollout (ROLLOUT_PHASE_2 §2.6): steps 12–13 done against prod Supabase 2026-09-10** — chain `0000`–`0006` applied (journal 7/7, all 27 tables, roles seeded), backfill run + `--verify-only` `countsMatch`/`sumsMatch` `true` (DB greenfield — 0 real rows). Steps 14–15 (parity window + `LEGACY_PROGRESSION_READONLY`) await a live deployment against this DB. | [Phase 2](./phases/PHASE_2_CORE_ARCHITECTURE_AND_DATA_PLATFORM.md) |
| 3 | Learning Product, MVP, Rebrand & Motion | **DoD §25: 17/19 met, 2 non-agent-completable (2026-09-21).** WS1–WS9 code-complete/merged (see `.claude/plans/phase-3-learning-shell-rebrand.md` for full per-workstream evidence). **WS10 close-out** (`phase-3/ws10-rollout-closeout`, stacked on PR [#29](https://github.com/DenisPoselyanov/biblegames_bot/pull/29)): a11y (§17: contrast incl. a follow-up sweep closing the deferred `--gold-light` text/icon contrast gap across 21 files/62 declarations, touch targets, focus-visible, route-heading focus, text-scaling-to-200%, ARIA live), performance (§18: code-splitting, lazy question-bank loading, render-blocking font fix, bundle-boundary + perf-budget CI gate), analytics (§19: 8/8 spec categories instrumented, payload allowlisting) all closed in earlier WS10 PRs. This close-out additionally: closed 2 of §23's unit-test gaps (`eventDedup.test.ts`, `motionCapabilities.test.ts` — the latter required extracting `MotionProvider`'s inline capability derivation into a pure, testable function since this repo has no React-component-rendering harness); **flipped all 8 rollout flags to their full-rollout default in code** (`learningShellV2`, `today_dashboard`, `learning_plans`, `lesson_experience_v2`, `practiceSessionV2`, `progress_dashboard_v2`, `profileSettingsV2`, `lightThemeDefault` — see [ROLLOUT_PHASE_3.md](./ROLLOUT_PHASE_3.md)), a product-owner decision made in place of §20's live staged-percentage rollout, which needs production metrics/ops access this repo's agent doesn't have; verified rollback live (flags off → legacy 4-tab shell renders correctly) and the flag flip itself (fresh load → v2 shell + light theme, no `.env.local` overrides). **Known, explicitly-not-hidden gaps** — full detail in [PHASE_3_TO_4_HANDOFF.md](./phases/PHASE_3_TO_4_HANDOFF.md): (§25.2) no learning content is `published` yet, so the live v2 screens are correct but empty until Phase 4 ships a publish step; (§25.14) no real-device Telegram Android/iOS verification, only emulated mobile widths — deferred to Phase 7; (§25.16) **no visual regression suite exists in this repo at all** — never built, not a Phase 3 regression, a fresh tooling decision needed later; (§23 "AnswerOption states") the one remaining required unit-test item with no non-component way to cover it absent a rendering harness; (§25.19) product-owner sign-off is, by construction, not agent-completable — pending. | [Phase 3](./phases/PHASE_3_LEARNING_PRODUCT_REBRAND_AND_MOTION.md) |
| 3.5 | Design v2 Visual Migration (Hallow-style rebrand) | **code complete (WS1–WS7 landed 2026-09-22); owner final visual sign-off (§11.10/§25.5) still pending — not agent-completable.** Started 2026-09-21. Owner-approved interstitial phase, not in the original master-specification numbering. `proto/design-v2` (`/proto`, `src/proto/README.md`) confirmed as the **final production direction**, superseding `DESIGN_RULES.md`'s current `Світло`/premium-spiritual-minimalism identity. Scope: re-skin every screen already shipped by Phase 3 (same IA, same reward logic, same server contracts — presentation layer only), rewrite `DESIGN_RULES.md`, re-run the WS10 §17/§18 accessibility and performance gates against the new palette. **ADR-018 accepted**: Tailwind v4 and `lucide-react` rejected (existing CSS-Modules/custom-property pipeline and hand-rolled `Icon.tsx` reused instead); `framer-motion` was already a production dependency (ADR-010) — no new decision needed there. **WS1 (Palette/token foundation) landed**: new `progressRampEnd` semantic token (gold-only-in-dark ramp end, §4), two new free catalog entries `aurora`/`aurora-light` pinned from `proto.css`'s locked dark/light values, `designSystemV2` flag (default off) wired into `resolveDefaultCosmeticThemeId()`. Owner decision: `light`/`classic` stay selectable post-migration (not retired). Verified in the dev browser — both new themes render correctly through the existing `src/components/ui/*` contracts with no screen changes to the current default. **WS2 (Core navigation shell) landed**: three new `auroraColor1/2/3` semantic tokens (default `transparent` on every pre-existing theme, pinned on `aurora`/`aurora-light` from `proto.css`'s `--p-aurora-1/2/3`); new `ShellAurora` component (drifting blurred radial-gradient blobs + grain, drift gated on `useMotionCapabilities().effectiveIntensity` not a raw media query) mounted by `AppShellV2` only behind `designSystemV2`; `BottomNavigation` gained an opt-in `variant="aurora"` floating-glass-pill style (`AppShellV2` passes it only when the flag is on; every other consumer keeps the default edge-to-edge bar); `ProfileV2` gained a "Вигляд" dark/light `SegmentedControl` (flag-gated) that quick-switches between the `aurora`/`aurora-light` catalog themes. Verified in the dev browser both ways: flag on renders the aurora background + floating nav + appearance toggle across Today/Навчання/Профіль with the dark↔light switch working live; flag off reproduces the exact pre-WS2 flat nav bar (theme color selection itself is unaffected either way, per WS1). **WS3 (Today + Learn) landed**: hero-card `tone="cover"` full-bleed pattern (new `CoverArt`/`Pill` primitives, `Button`'s `onColor` variant) applied to Today's active-lesson/due-review/first-time hero states and to `LearningHub`'s plan-browse list, `PlanDetail`, `ModuleDetail`, `LessonSession`; `fontSerif`/`fontSans` promoted to theme-aware `SemanticPalette` fields (Literata/Manrope pinned only on `aurora`/`aurora-light`). **WS4 (Practice/Review + Play hub) landed**: the same `HeroCard`/`CoverArt`/numbered-row patterns applied to `PlayHub` (featured mode as a full-bleed hero, the rest as `CoverArt`-fronted rows), `PracticeIntent` (plan step gets the `LearningHub`-style cover-card list, module/lesson steps get `PlanDetail`/`ModuleDetail`-style numbered rows), `ReviewHub` (due-count stat card, reason-icon leading rows) and `PracticeSession` (segmented per-question progress bar mirroring `LessonSession`'s, non-scrolling viewport-sized result screen with an accuracy `ProgressRing` computed client-side from the session's own answers — no XP/coin totals shown since the practice-session contract returns none). Does not touch Millionaire/Survival/Kahoot's own screens or WS9's score-authority fix (Phase 3) — this pass only covers the Practice/Review flow and the Play hub picker screen, per the plan's explicit scope note. Verified in the dev browser both flag states and both aurora themes; not verified against the live practice-session API (prod Supabase — reading published plans is safe but creating a session is a write, avoided per the same precedent Phase 2/3 already documented). Blocks nothing — Phase 4/Content Studio can proceed in parallel since Studio is already specified against the same tokens (see Phase 4 §10 note below). **WS5 (Progress + Profile/Settings/Themes) landed**: `ProgressV2`'s rank/streak header becomes a `HeroCard tone="cover"` (new `ProgressRing` `onColor` variant for legible white stroke/text on the scrim) and its "continue lesson" card gets the same `CoverArt` list-card treatment as `LearningHub`/`PracticeIntent`; `ProfileV2` and `Settings`' theme rows get a new small `ThemeSwatch` indicator (live preview dot of the equipped theme) replacing the generic star icon. `ThemePicker` itself needed no code change — its grid already re-skins for free through the existing token pipeline (no gradient-panel/flat-row antipattern to replace there, unlike the other three screens). Verified in the dev browser, flag on/off and both aurora themes. **WS6 (Shop/Social secondary routes) landed**: `Shop`'s and `CosmeticThemeShop`'s avatar/theme cards move off the legacy glass-blur card (§11.5 — glass is a controlled exception, not the base surface) onto the standard `ContentCard` surface, with `Pill`/`Button` replacing the bespoke balance pill, badge and apply/buy button classes. Across the four `src/pages/social/*` screens (`Communities`, `CommunityDetails`, `Challenges`, `ChallengeDetails` — none of which had any `designSystemV2` branching before this workstream), section wrappers become `ContentCard`, list rows (communities, members, pending/history challenges) become `ListRow`, status/count chips become `Pill`, primary actions become `Button` (including its `danger` variant for "Видалити спільноту"), the community search input becomes the shared `SearchField`, the 3-option challenge-difficulty picker becomes a `SegmentedControl` (§9.5/§13.3), and empty states use the existing app-wide `EmptyState` component instead of bespoke emoji blocks. No entitlement/reward logic touched — same forbidden-shortcut boundary as Phase 3 §24/§27 and this phase's own §10. Verified in the dev browser: flag on renders every screen's new `ContentCard`/`ListRow`/`Pill`/`Button` treatment correctly including with live-created data (a real community and its member list); flag off reproduces the exact pre-WS6 legacy layout (confirmed by toggling `VITE_FLAG_DESIGNSYSTEMV2` off and back). Not verified: the received-challenge/history `ListRow` states specifically (`friendChallengeManager`'s local dev-storage write did not persist under the sandboxed preview, a pre-existing local-storage quirk unrelated to this pass — the same `ListRow`/`Pill` primitives are already confirmed rendering real data elsewhere in this workstream). **WS7 (Accessibility + performance re-audit, flag flip, rollback verification, `/proto` retirement) landed**: full WCAG AA contrast re-audit of `aurora`/`aurora-light` (new `src/lib/paletteContrast.test.ts` regression suite) found and fixed five real failures — `textMuted` on both themes (3.56:1/2.80:1 → ~4.9:1/4.85:1), the primary-button gradient's text contrast at 14px/700 (below the "large text" floor) on both stops (4.47:1/3.96:1 → ~4.6:1, hue/direction unchanged), a missing `textLink`/`buttonSecondaryText` pin on `aurora` (generic fallback to raw `brandPrimary` measured 4.41:1), and — the significant one — `--state-success-text`/`--state-danger-text` (correct/incorrect answer feedback color), which turned out to be *static, theme-independent* globals tuned only for a dark canvas (~1.2–1.3:1 on any light theme's own tinted background, essentially invisible); promoted into the per-theme `deriveSemanticPalette()` pipeline with an `isLight` branch, fixing `aurora-light` **and retroactively fixing the same pre-existing bug in the already-shipped Phase 3 `light` theme** (dark themes `classic`/`aurora` are byte-identical to before — regression-tested). Performance: `npm run check:bundle-size` re-run clean (208.7KB/260KB JS, 30.6KB/40KB CSS) — confirms ADR-018's "no new dependencies" holds after all 6 re-skin workstreams. Flag: `designSystemV2` flipped to default `true` in `src/lib/flags.ts`; rollback verified live both directions (flag on → fresh session defaults to `aurora`; flag off + cleared storage → exact pre-migration `Світло` screen, byte-for-byte the old look). `/proto` retirement: confirmed `src/proto/*` was never merged into this branch or `main` at any point — the click-through prototype only ever existed on the separate, never-merged `proto/design-v2` branch; every visual decision was hand-translated into production tokens by WS1–WS6, so there was no route to unregister. `docs/DESIGN_RULES.md` rewritten in full for the new system (not patched) per §8; `docs/MOTION_SYSTEM.md` left untouched (ADR-018 — no new motion primitives). **WS8 (post-flip visual audit follow-up, not in the original WS1–WS7 plan)**: a full-screen sweep after WS7's flag flip found several `designSystemV2` screens still missing the hero-card/`CoverArt` treatment the rest of the phase established — `Today` gains a personalized greeting header and a "Швидкий підхід" quick-practice/quick-review tile row, `PlayHub`'s "Усі режими" list becomes a 2-column full-bleed card grid (plus adopts the shared `AppPage`/`PageHeader` shell both flag states already use elsewhere in the app), `LessonSession` gains a cumulative block-stack view (scroll instead of one-block-at-a-time) with `richBlocks` scripture-parchment/insight-callout styling and a post-completion dialog routing into Practice, `ProfileV2` gains an avatar-in-progress-ring hero header, and `Shop`/`Communities`/`Challenges` gain illustrated hero banners/`CoverArt` avatar cards. All additions are gated behind `designSystemV2` (confirmed during review — the initial pass had the Today greeting/quick-tiles unconditional, fixed before commit to preserve §11.4's flag-off-exact-rollback contract). Frontend `tsc -b` clean; full `npm run test` run shows only 5 pre-existing server-side integration-test timeouts unrelated to this (zero server files touched). Verified live in the dev browser: `PlayHub`, `Shop`, `ProfileV2`, `Communities`, `Challenges` all render correctly against a local server. `Today` and `LessonSession` could not be exercised live — the local JSON content store has no published learning plans/lessons seeded (the same content gap Phase 3's own status entry already documents for production) — so those two rely on `tsc`'s clean pass plus manual code review rather than a rendered screenshot. **WS9 (side-by-side prototype parity pass, 2026-09-22)**: owner review of the flipped build against `/proto` asked for the screens themselves to match the prototype, not only its tokens. Ran `proto/design-v2` and the production app side by side at 390×844 and closed the composition gaps: prototype tab set/labels/glyphs and raised-pill active state (with `navActive`/`navInactive` pinned on both aurora themes), Millionaire/Survival/Kahoot-lobby moved into the fullscreen-route set (§5.4 already listed Millionaire), a `data-design-v2` token block in `src/index.css` carrying the prototype's radii and type scale, page headers switched from gold kicker to serif title + one muted line, Today's streak/daily-goal card and verse-of-day card, Progress's rank-ring summary and real-`studyMastery` mastery list, Profile's in-card stat trio + colour-theme swatches, Play featuring Мільйонер (Дослідження dropped — its route only redirects to Learn), Millionaire/Survival rebuilt as a single reading column instead of a header + empty half-screen + bottom sheet, Practice/Lesson reader headers, and Kahoot/Shop/Social headers and grids. Deliberately not ported: Progress's activity heatmap and Learn's per-plan progress meters — no server data source exists for either, and inventing one would break the phase's own rule. `tsc -b` clean, 467/467 tests pass, no horizontal overflow on any tab route, verified live in both aurora themes. Today/LessonSession still cannot be exercised with real content locally (no published plans seeded); Today's composition was verified against a temporary local fixture that was reverted afterwards. Remaining: owner's final visual review (§11.10) — not something an agent session can complete. | [Phase 3.5](./phases/PHASE_3_5_DESIGN_V2_VISUAL_MIGRATION.md) |
| 4 | Content Quality, Reviewed AI Pipeline & Content Studio | in progress — WS1–WS7 merged to `main` by 2026-09-23 (PR [#33](https://github.com/DenisPoselyanov/biblegames_bot/pull/33)→[#38](https://github.com/DenisPoselyanov/biblegames_bot/pull/38): content lifecycle, validation pipeline, RBAC hardening, publication rollback, audit trail); WS8a (Studio scaffold/dashboard/jobs, PR [#39](https://github.com/DenisPoselyanov/biblegames_bot/pull/39)) merged; WS8b (review queue/editor, PR [#40](https://github.com/DenisPoselyanov/biblegames_bot/pull/40)) open; WS8c (library/releases/settings, PR [#42](https://github.com/DenisPoselyanov/biblegames_bot/pull/42)) and WS9 (quality feedback loop: answer-accuracy outliers, player content reports, AI repair jobs; migration `0013` not yet applied to production) and WS10 (`npm run ai` unified CLI, legacy bank audit — see [LEGACY_CONTENT_AUDIT.md](./LEGACY_CONTENT_AUDIT.md) — migration waves tooling, script deprecation matrix — see [CONTENT_TOOLING.md](./CONTENT_TOOLING.md); waves not yet applied to production) code complete, stacked on WS8b. This status line predates that work and is not a per-workstream account — see PR history and `.claude/plans/phase-4-content-ai-studio.md` for the authoritative breakdown. **Design/IA locked 2026-09-21**: the `proto/design-v2` branch's `/proto/studio` click-through prototype (`src/proto/studio/README.md` + `ROADMAP.md`, synthetic data only, not yet merged) is now the concrete screen/motion reference for Content Studio (see [Phase 4](./phases/PHASE_4_CONTENT_AI_AND_CONTENT_STUDIO.md) §10/§15) and legacy `/admin` (`AdminPanel.tsx`) is confirmed **retired into Studio**, not redesigned in place (see [PHASE_3_TO_4_HANDOFF.md](./phases/PHASE_3_TO_4_HANDOFF.md) item 6). `ROADMAP.md`'s WS1–WS9 is the recommended execution breakdown for actually building it. **Lesson content variety (2026-09-23, branch `phase-4/lesson-block-types`)**: eight new interactive lesson block types (`true_false`, `order_events`, `fill_blank`, `match_pairs`, `reveal`, `scenario`, `character_card`, `memory_verse`) plus advisory lesson archetypes added on top of Phase 3's block schema table — see [§5.3.1](./phases/PHASE_4_CONTENT_AI_AND_CONTENT_STUDIO.md#531-interactive-lesson-block-library-2026-09-23). Also removed the broken pointer-shaped `question` block the Phase 3 topic-tree mapping script used to emit (never resolved to a real question client-side); a `migrate:drop-legacy-question-blocks` script cleans up existing rows, not yet run against production. Server-backed `poll` (community results) and a persisted `reflection` answer were considered and deliberately deferred — both need a schema change. | [Phase 4](./phases/PHASE_4_CONTENT_AI_AND_CONTENT_STUDIO.md) |
| 5 | Social, Groups, Challenges & Multiplayer | planned — private communities і async challenges спочатку; persistent/reconnectable Kahoot після них | [Phase 5](./phases/PHASE_5_SOCIAL_GROUPS_CHALLENGES_AND_MULTIPLAYER.md) |
| 6 | Economy, Shop, Entitlements & Monetization | planned; monetization model not selected — immutable ledger, entitlements і no-pay-to-win | [Phase 6](./phases/PHASE_6_ECONOMY_SHOP_ENTITLEMENTS_AND_MONETIZATION.md) |
| 7 | Performance, Offline, Accessibility & Public Release | planned — offline capability matrix, media/CDN, accessibility, observability, load tests і release gates | [Phase 7](./phases/PHASE_7_PERFORMANCE_OFFLINE_ACCESSIBILITY_AND_RELEASE.md) |
| 8 | Expansion and Bonus Capabilities | optional future — advanced adaptive learning/FSRS, Church/Classroom, bounded AI assistant та інші owner-approved options | [Phase 8](./phases/PHASE_8_EXPANSION_AND_BONUS_CAPABILITIES.md) |

## Canonical MVP

Перший production MVP завершується всередині Phase 3 після виконання залежностей Phase 1–2. Він має довести один повний server-authoritative цикл:

```text
published lesson
→ checkpoint
→ practice session with frozen question revisions
→ feedback and explanation
→ mistake review
→ objective mastery
→ progress dashboard
```

У цей MVP входить «Мільйонер» як перша гра на спільному Question Runtime. Live Kahoot, відкриті communities, Stars/Premium, user-facing AI chat, full offline rewards і Redis-based horizontal realtime не є MVP blockers.

## Важливе розмежування

Старий `docs/product-rebuild/MASTER_ROADMAP.md` мав іншу нумерацію Phase 0–13. Його Phase 0–7 були реалізовані в коді й залишаються частиною актуального baseline, але **не означають**, що канонічні Phase 0–7 завершені. Відповідність і правила переходу описані в [ROADMAP_MIGRATION.md](./ROADMAP_MIGRATION.md).

## Binding domain-документи

- [OPEN_SOURCE_REFERENCE_ARCHITECTURE.md](./OPEN_SOURCE_REFERENCE_ARCHITECTURE.md) — stack, modular monolith, Question Bank/Runtime, Lesson Blocks, MVP, reference repos і search guide.
- [PHASE_3_REBRANDING_AND_THEME_SYSTEM.md](./PHASE_3_REBRANDING_AND_THEME_SYSTEM.md)
- [DESIGN_RULES.md](./DESIGN_RULES.md)
- [MOTION_SYSTEM.md](./MOTION_SYSTEM.md)
- [MONETIZATION_STRATEGY.md](./MONETIZATION_STRATEGY.md)
- [FUTURE_UPGRADE_OPTIONS.md](./FUTURE_UPGRADE_OPTIONS.md) — лише каталог кандидатів, не roadmap.

## Reference rule

Oppia, H5P, Moodle, ClassQuiz, Anki/FSRS, Frappe Learning, Kolibri й AndBible є архітектурними референсами, а не dependencies. Будь-яке копіювання коду потребує окремої license review; за замовчуванням патерн реалізується нативно в поточному React/TypeScript/Express/PostgreSQL стеку.

Статус `completed` дозволено встановити тільки після виконання acceptance criteria, migrations, tests, rollout/rollback і наявності evidence у коді.
